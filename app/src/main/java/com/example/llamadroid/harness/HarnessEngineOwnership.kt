package com.example.llamadroid.harness

/** Legacy services remain reusable for manual file tools, but cannot execute model turns. */
object HarnessEngineOwnership {
    val legacyExecutionRetired: Boolean get() = true
}
